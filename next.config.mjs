/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: true,
  //output: "export",
  images: {
    unoptimized: true,
  },
  // basePath: "/company",
}; 

export default nextConfig;
